name: Release

on:
  push:
    tags: v*

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Release
      uses: softprops/action-gh-release@v0.1.13
      with:
        fail_on_unmatched_files: true
        files: |
          **/*.yml
          **/*.json
